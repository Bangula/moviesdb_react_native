import React from "react";
import {
  View,
  ScrollView,
  Text,
  Button,
  StyleSheet,
  SafeAreaView
} from "react-native";

const Home = ({ navigation }) => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Home Page</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Home;
